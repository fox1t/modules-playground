const index = require('./build')

/**
 * These export configurations enable JS and TS developers
 * to consume modules in whatever way best suits their needs.
 * Some examples of supported import syntax includes:
 * - `const helloWorld = require('./ts-triplet')`
 * - `const { helloWorld } = require('./ts-triplet')`
 * - `import * as HelloWorld from './ts-triplet'`
 * - `import { helloWorld, TSC_definition } from './ts-triplet'`
 * - `import helloWorld from './ts-triplet'`
 * - `import helloWorld, { TSC_definition } from './ts-triplet'`
 */
module.exports = Object.assign(index.default, index)